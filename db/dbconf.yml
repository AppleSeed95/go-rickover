development:
    driver: postgres
    open: user=rickover dbname=rickover sslmode=disable

test:
    driver: postgres
    open: user=rickover password=rickover dbname=rickover_test sslmode=disable

github:
    driver: postgres
    open: user=rickover password=hunter2 dbname=rickover_test sslmode=disable

cluster:
    driver: postgres
    open: $DATABASE_URL
